import { RegexMarkDirective } from './regex-mark.directive';

describe('RegexMarkDirective', () => {
  it('should create an instance', () => {
    const directive = new RegexMarkDirective();
    expect(directive).toBeTruthy();
  });
});
